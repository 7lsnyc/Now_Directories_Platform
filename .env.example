# Supabase Configuration
# Required for authentication and database access
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Application Configuration
# Controls behavior of the platform
NODE_ENV=development  # Options: development, production, test
DEFAULT_DIRECTORY_SLUG=notary  # Default directory if none is specified by domain

# Optional Configuration
# These have sensible defaults but can be customized
ENABLE_ANALYTICS=false  # Set to 'true' in production
DEBUG_MODE=true  # Set to 'false' in production
API_TIMEOUT_MS=10000  # Default timeout for API calls in milliseconds

# External Service API Keys (Optional)
# Add these as you integrate with additional services
# GOOGLE_MAPS_API_KEY=your-google-maps-key
# STRIPE_PUBLIC_KEY=your-stripe-public-key
# STRIPE_SECRET_KEY=your-stripe-secret-key
